// opposite number
// given an integer or a flaoting point number, find its opposite
function opposite(number) {
    return(-number);
  }

//OR
const opposite = number => -number;

//OR
function opposite(number) {
    return number * (-1);
}